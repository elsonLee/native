syntax = "proto2";

//! deprecated
option cc_generic_services = true;

package testcase;

message Request {
    repeated int32 num = 1;
}

message Reply {
    optional int32 sum = 1 [default = 0];
}

service SumService {
    rpc Solve (Request) returns (Reply);
}
